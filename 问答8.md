整体上，这个 `tactile-only` 方案是合理的，而且这次边界划得比较干净：**同一个触觉任务，只拿掉 1-back，不重新发明另一套实验。** 我认为可以做。

但在真正动代码前，我会提醒 Codex 处理好三个问题。除此之外，不建议继续扩展范围。

第一个问题，也是最重要的，是“single 和 dual 的时间可比性”。

Codex 现在的定义是：

> dual：9–13 s gap + trial window + digit guard
> tactile-only：9–13 s gap，但没有 trial window 和 digit guard

从程序结构上完全合理，但实验上意味着两个条件并不是绝对只有“有没有1-back”这一个差异。dual 中触觉事件偶尔会因为 trial window 或 digit onset guard 被延迟。

不过根据我们前面的1000次模拟，`[9000,13000]` 下 trial-window 本身几乎不介入，所以我认为这个问题目前不需要通过重新设计 single timing 来解决。正确做法是：**保持两种条件同一个 gap distribution，并验证最终实际 timing 是否相近。**

因此我建议日志里至少继续保留：

```text
planned_onset_ms
actual_emit_ms
previous_event_actual_emit_ms
sampled_gap_ms
held_by_trial_gate
delayed_by_digit_guard_ms
```

然后 pilot 后直接比较：

```text
single vs dual
实际 inter-event interval 分布
整个 contact -> release 时长
P1-P5 的相对位置
```

如果两边实际时间分布差不多，就不需要再处理。如果 dual 系统性比 single 长很多，才需要重新讨论。

**不要为了形式上的完全一致，在 tactile-only 里模拟一个假的 digit guard。** 那反而没有意义。

---

第二个问题是：

> `current_trial_number=None` 时，由谁负责忽略 `nback_trial_window`？

这里我建议不要让这个行为变得隐式。

不要写成：

```python
if current_trial_number is None:
    好像自然就跳过了
```

而应该明确由 `task_type` 决定：

```python
trial_gate_enabled = task_type == "dual"
digit_guard_enabled = task_type == "dual"
```

然后日志记录：

```text
trial_gate_enabled = false
trial_gate_ignored = true
```

原因是以后调试时非常清楚：

> 这是因为 tactile-only 按设计关闭了 trial gate，

而不是：

> trial number意外变成None导致gate失效。

这是软件可靠性问题，不是实验设计问题，但正式采集时很重要。

同样，dual 模式下如果 `current_trial_number=None`，反而应该视为异常，而不是静默跳过 trial gate。

---

第三个问题是 session 的结束条件。

Codex 写：

> release结束后 → `post_release_recording_ms` → session结束

这个总体对，但我要把“release结束”定义清楚。

不要让 runner 依赖：

> 被试完成了 release 行为

才能结束。

实验 timeline 的结束应该依赖：

> **release haptic event已经发送完成**

然后再记录固定的 `post_release_recording_ms`。

因为被试如果 release 做错、没做、动作很慢，这些本身都是行为数据。不能因为错误反应导致整个实验无限等待或者改变session长度。

也就是说：

```text
release cue emitted
↓
release stimulus finished
↓
post_release_recording_ms
↓
session end
```

行为分析事后再判断：

```text
release first_response_correct?
release sequence_complete?
```

刺激调度和行为正确性不要耦合。

---

除此之外，我基本赞成 Codex 的方案。

尤其这几个决定我认为应该保留：

> 不新建 `run_tactile_only.py`；
> 不复制 scheduler；
> 不复制一套 tactile plan；
> 不改 `[9000,13000]`；
> 不改变 dual 现有默认行为；
> tactile-only 忽略 `nback_trial_window`；
> tactile-only 仍完整采集 pinch/wrist/haptic 数据。

这才保证以后：

> `task_type = tactile_only`

和：

> `task_type = dual`

真的是同一套触觉任务。

关于空的 `nback_events.csv`，我也赞成保留 header-only 文件。它会让后面的批量分析简单很多。对应 summary：

```text
task_type: tactile_only
nback_enabled: false
nback_trial_count: 0
```

非常明确。

不过后续分析脚本不要看到 `nback_events.csv` 存在就假设有1-back，而应该以：

```text
nback_enabled
task_type
```

作为判断依据。

---

还有一个命名细节我建议现在就处理。

你最终论文里实际上是：

```text
Task Type:
single
dual
```

所以程序内部虽然 Codex 想叫：

```text
tactile_only
dual
```

也可以，但最终分析数据最好统一映射成：

```text
task_type = single
task_type = dual
```

否则以后统计代码里会出现：

```text
tactile_only vs dual
```

而论文Methods写：

> single-task vs dual-task

虽然不是错，但容易产生不必要的不一致。

我个人甚至倾向直接配置：

```yaml
task_type: single
```

和：

```yaml
task_type: dual
```

然后：

```python
nback_enabled = task_type == "dual"
```

更加直接。

---

因此，这一轮代码工作的目标我会给它划成：

> **只让现有 runner 支持 `single / dual` 两种 task type。Single保留完整触觉任务和传感记录，仅关闭1-back及所有依赖1-back的调度约束。不得修改触觉行为 detector、pinch三状态分析、scheduler timing参数或现有dual行为。**

验收条件也很简单：

同一个 haptic plan，分别跑 single 和 dual，必须确认：

1. 两边都完整发送7个 semantic cue；
2. cue语义顺序相同；
3. 都使用 `[9000,13000]`；
4. single不存在任何实际 n-back/trial-window/digit-guard行为；
5. dual行为与修改前完全一致；
6. single的 pinch/wrist/haptic日志能够直接进入现在的统一行为分析；
7. summary明确区分 `task_type`；
8. 两种模式实际触觉时间分布没有出现明显系统性偏差。

这8项通过，这个功能就可以停止修改。

至于 Open/C-shape/Pinch、first-excursion、calibration reuse 等内容，**都不要顺便塞进这一次 tactile-only 改动里。** 它们分别属于行为分析和calibration管理，是另外的 milestone。

所以这次 Codex 的方案我认为可以执行，主要只需要补上我上面说的三个边界：**明确控制 trial gate、明确 release 后的结束条件，以及 pilot 后检查 single/dual 实际触觉 timing 是否真的可比。**
