目前 MANUS 数据路径是：

`SDKMinimalClient_Windows → TCP newline JSON → Python LiveRawStreamServer → _frames deque FIFO → calibration/formal loop`

现在怀疑的问题是：在 calibration prompt 等待用户按 Enter 时，后台接收线程仍然持续把 MANUS frame 放进 `_frames`。因此用户虽然已经做好手势才按 Enter，但 calibration 开始后通过 `popleft()` 读取的仍可能是 Enter 之前积压的旧帧和 transition frames。

这可以解释目前 `calibration_timeseries.csv` 中：

* `contact` 开头先出现接近 Open 的 `pinch_distance ≈ 0.135`，随后才下降到稳定的 `≈ 0.042`
* `pinch` 开头先出现接近 Contact 的 `≈ 0.041`，随后才下降到稳定的 `≈ 0.013`

请先不要大改数据流程。先增加 `queue_depth` 的诊断记录，用来确认是否存在 queue backlog。

具体希望：

1. 在关键时间点记录 `len(self._frames)`，至少包括：

   * calibration 每个 phase 按 Enter 后、开始采集前
   * formal task/session 开始时
   * 如果方便，formal loop 运行期间也周期性记录

2. 将 `queue_depth` 连同：

   * `monotonic_ms`
   * `frame_index`
   * `phase`
   * `pinch_distance`

   一起写入 timeseries CSV。

3. 最好额外记录：

   * `queue_depth_at_phase_start`
   * 当前读取到的 `frame_index`
   * 如果已有的话，`latest_received_frame_index`

目标是判断：

* 如果 queue 只在 prompt 阶段积累，formal loop 中长期接近 0–1，那么问题主要是 phase boundary backlog。
* 如果 formal loop 中 queue_depth 也持续增长，那么正式 RT 也可能受到持续的数据延迟影响。

暂时先以诊断为主，不要直接用“固定删除前 1 秒”的方式修数据。
