我把三个输出文件都实际读了一遍，也重新对照了原始 zip 里的 wrist/pinch 连续数据。总体判断是：**Codex 这轮实现是有效的，尤其 neutral-centered 重分类已经基本验证了我们之前对旧 gate 的判断；但在进入 live gate 修改之前，还有两个分析脚本层面的点必须先修。**

`summary` 里现在有84个 semantic cue，dual和matrix-only各42个；其中49个 `clean`、1个 `recoverable`、9个 `contaminated`、1个 `no_pinch_increase`，contact/release共24个暂不评分。 排除 contact/release 后，实际需要行为评分的是60个cue，所以目前是49/60=81.7% clean；如果把那1个 recoverable也算作可用于部分分析，则50/60=83.3%。

### 1. Neutral-centered classifier 的诊断结果相当支持修改 online gate

48个方向cue中，旧 online gate 有20次 `False`。这20次里面，新 neutral-centered 判定有 **17次重新认为实际上处于neutral**，也就是85%。

而且反方向没有出现明显问题：

> online gate = True 的28次，neutral-centered 28次全部仍然判为neutral。

所以目前的结果不是“新算法为了救数据把什么都判成neutral”，而是非常有针对性地恢复了旧算法漏掉的一批neutral。

我还重新检查了校准关系：

* LR维度：48个cue中44个对应的旧 neutral region包含0；
* UD维度：只有24/48对应的旧 neutral region包含0。

因为每个session有4个方向cue，这基本就是我们之前发现的：

> **12个session中，有6个session的up/down旧neutral区根本不包含真正的neutral=0。**

而新算法的LR/UD sanity check在48个cue中全部通过。

所以现在已经可以比较有底气地说：

> **旧 online neutral gate 确实存在结构性误判，尤其集中在up/down轴；neutral-centered classifier至少作为 online gate 的替代基础是合理的。**

剩余真正被新算法判为 non-neutral 的只有3/48：

* dual-5 的 `up`：cue发出时仍是right；
* dual-5 的 `down`：cue发出时仍是right；
* matrix-only-1 的 `left`：cue发出时仍是right。

这三次就不应该强行通过后处理“洗成clean”。其中最后一个后来确实完成了left，所以脚本把它标成 `recoverable`，这个处理逻辑是合理的。

### 2. 现在真正突出的问题已经从“neutral gate”转移到了 `up`

这是这批新结果里我认为最重要的新发现。

方向cue结果是：

| Cue   | clean/recoverable |                 问题 |
| ----- | ----------------: | -----------------: |
| right |             12/12 |                  0 |
| left  |             11/12 |     1 contaminated |
| down  |             10/12 |     2 contaminated |
| up    |          **6/12** | **6 contaminated** |
| slip  |             11/12 |         1 no-pinch |

如果把 `up` 暂时拿掉，其余4类有行为反应的cue是：

> **44/48 = 91.7% 可用。**

所以现在已经不像“整个动作识别系统都很差”，而是异常明显集中到了 **up**。

而且我又回到原始 `wrist_rotation_timeseries.csv` 看了这些失败的up。这里非常关键：

**多数不是分析脚本把真实up误判成down，而是受试者cue后真的做出了稳定的down动作。**

例如 dual-2 那个失败up：

> cue后约0–1.83 s仍neutral
> → 短暂进入up约47 ms
> → 约1.91 s开始进入down
> → 然后连续down超过3秒。

dual-3的几个失败up也类似：

> neutral → 稳定down → 回neutral。

matrix-only-1那个失败up也是：

> neutral → down持续约700 ms → 后面才有很短的up。

所以这6个up contaminated **不能再归因于neutral classifier**。后处理也不应该把它们“修正成正确”。

现在应该检查的是另外一层：

> dual条件里的 `up` tactile pattern到底是什么；
> up/down 的物理刺激编码有没有可能被混淆；
> plan中的 `command_id / command_label` 有没有一致；
> 学习阶段这个参与者对up的掌握情况如何。

因为6个失败up中：

> dual：5个失败、只有1个clean；
> matrix-only：1个失败、5个clean。

这个分布很显眼。

但现在只有一个参与者，所以**不能据此马上说dual反馈里的up设计有问题**。它现在应该被定义成下一个pilot需要重点检查的候选问题。

这也说明新脚本实际上是有用的：旧算法时代我们会把注意力全放在“怎么这么多人没neutral”；现在neutral问题被拆掉以后，真正的cue-level行为错误开始显现出来了。

### 3. Slip detector 目前表现也合理

12次slip：

> 11次 `clean`；
> 1次 `no_pinch_increase`。

那一次失败是 matrix-only-4，baseline closure约0.523，peak只有约0.542：

> Δclosure ≈ 0.019

而正常的11次：

> Δclosure大约0.22–0.55。

这个分离非常大，所以至少对这个参与者来说，`pilot_v0.1` 并没有处在一个“阈值稍微变一点结果就全变”的危险区域。

而且 Codex 修改后的 release 判断也起作用了。11个clean slip里，有好几个：

> `returned_to_precue_baseline=False`

但仍然正确识别了明显的 pinch → reopening。

这正是我们想要的。

所以 slip 这一块暂时不需要继续调参数。等再有2–3个参与者以后再冻结阈值即可。

---

不过当前 `cue_response_metrics.csv` 还有两个我认为应该马上让 Codex 修的小问题。

第一个是 **quality标签存在一个内部不一致**。

matrix-only-1 有一个 `left` 被标为：

```text
trial_quality = clean
quality_reason = neutral_return_not_detected_before_next_cue
full_cycle_ms = NaN
```

也就是说它一方面说clean，一方面又说没有检测到return-to-neutral。

这需要明确你究竟把 `clean` 定义成：

> 正确方向反应已经检测到；

还是：

> 正确方向反应 + 完整回neutral周期都检测到。

我更建议不要硬揉成一个标签，而是拆成：

```text
response_quality
cycle_quality
```

比如：

```text
response_quality = clean
cycle_quality = incomplete_return
```

因为对于Exp2：

> accuracy/RT可能完全可用；

但：

> full_cycle不能用。

没必要因此把整个trial都判废。

第二个问题更加重要：**recoverable trial现在的 `response_rt_ms=0` 不能进入任何正式RT统计。**

那个 matrix-only-1 left trial：

> cue出现时手已经在right
> 所以 `response_onset_ms = cue_onset_ms`
> `response_rt_ms = 0`

后来它确实纠正成left，因此 `eventual_correct=True`。

这对accuracy/correction分析是有价值的，但0 ms绝对不是“触觉反应时间”。

当前统一文件缺少一个我们之前讨论过的字段：

```text
correct_response_onset_ms
correct_response_rt_ms
correction_time_ms
was_corrected
```

我建议补回来。

以后逻辑应该明确：

> `first_response_rt_ms`：第一次新行为反应的时间；
> `first_response_correct`：第一次是否正确；
> `correct_response_rt_ms`：第一次正确行为出现的时间；
> `was_corrected`：先错后对。

如果cue onset时已经处于某一动作状态：

> first-response RT应该标为无法可靠确定，而不是0。

这样以后不会因为几个0 ms把RT均值拉坏。

还有一个很小但正式实验前必须修的字段：

> `participant_id` 现在84行全部是空的。

这批只有一个人当然不影响现在pilot，但你以后要做trial-level mixed model，participant ID是绝对不能缺的。最好让采集阶段的 `summary.json` 明确写participant ID，分析脚本从那里读，而不要靠文件夹名猜。

所以我现在给 Codex 的下一轮任务不会很大，主要是四件事：

1. 保留当前neutral-centered诊断结果，不继续调classifier；
2. 修正 `response_quality` 与 `cycle_quality` 的概念混用；
3. 加 `first_response_rt / correct_response_rt / correction`，禁止预存动作产生0-ms正式RT；
4. 补 `participant_id`。

然后**专门审查up**，而不是继续整体改 wrist detector。

特别是我建议让 Codex生成一张很简单的up诊断表：

```text
session
condition
plan
cue_position
expected = up
pre_cue_state
first_stable_direction
first_direction_rt
max_up_score_after_cue
min_down_score_after_cue
eventual_up_detected
```

再把6个失败up对应的原始轨迹画出来。

如果这些图和我刚才看到的一样，明确表现为“cue后真的朝down运动”，那么这批trial就应该被诚实地保留为**行为错误**，而不是当作算法污染删除。

这其实是一个好结果：现在数据开始从“分类器到底有没有坏”逐渐变成“参与者到底对哪个触觉信号做错了”。只有到这个阶段，Exp2的正式行为指标才真正开始可信。

总体上，这次 Codex 的工作我认为是成功的。**neutral问题已经基本定位；slip分析基本成立；下一步最大的未知数不再是回正，而是up为什么在这个参与者的dual条件下频繁产生down response。**
