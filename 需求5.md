当前不要进入 1-back 主循环集成，不要写 run_pinch_haptic_1back.py，不要接 MANUS live，也不要接真实 ESP32 TCP。

先修当前接口不一致：

1. haptic_trial_scheduler.py 已经依赖：
   - plan.timing.contact_onset_delay_ms
   - plan.timing.inter_event_gap_ms
   - plan.timing.refractory_ms
   - event.onset_delay_ms
   - event.onset_gap_after_previous_ms

2. tests/test_scheduler_sender_dry_run.py 也已经使用：
   - timing
   - event onset_delay_ms / onset_gap_after_previous_ms
   - sender.record_scheduled_event(event)
   - sampled_delay_ms
   - sampled_gap_ms

但是当前 haptic_plan_config.py 和 simple_haptic_sender.py 没有这些接口。
请先完成下面修改：

A. 修改 haptic_plan_config.py

新增 HapticPlanTiming dataclass：
- contact_onset_delay_ms: tuple[int, int]
- inter_event_gap_ms: tuple[int, int]
- refractory_ms: int

HapticPlanConfig 增加：
- timing: HapticPlanTiming

HapticPlanEvent 增加：
- onset_delay_ms: tuple[int, int] | None = None
- onset_gap_after_previous_ms: tuple[int, int] | None = None

haptic_plan_config_from_dict 的 allowed keys 增加 timing。
events[] allowed keys 增加：
- onset_delay_ms
- onset_gap_after_previous_ms

增加 range 校验：
- 必须是长度为 2 的 list/tuple
- 两个值必须是非负整数
- lower <= upper

to_dict() 必须能输出 timing、onset_delay_ms、onset_gap_after_previous_ms。

B. 修改 simple_haptic_sender.py

HAPTIC_EVENT_FIELDS 增加：
- sampled_delay_ms
- sampled_gap_ms

HapticEventRecord 增加：
- sampled_delay_ms: int | None = None
- sampled_gap_ms: int | None = None

_record_event() 增加参数：
- sampled_delay_ms
- sampled_gap_ms

新增方法：
record_scheduled_event(self, scheduled)

它接收 haptic_trial_scheduler.ScheduledHapticEvent，并根据 scheduled.event_name 分发：
- contact -> send_contact
- release -> send_release
- slip -> send_slip
- left -> send_matrix_left
- right -> send_matrix_right

必须完整传递：
- haptic_trial_index
- event_index
- command_label
- command_id
- channel_list
- duration_ms
- trigger_zone
- trigger_pinch_distance
- trigger_frame_index
- original_planned_onset_ms
- adjusted_onset_ms
- nearest_digit_onset_ms
- digit_onset_delta_ms
- onset_was_delayed
- sync_warning
- sampled_delay_ms
- sampled_gap_ms

C. 更新旧测试

tests/test_simple_haptic_sender_disabled.py 里的 plan config 也要补 timing。
因为 haptic_plan_config.py 修完后，timing 应该是必填字段。

D. 确认 haptic_plan_config_example.yaml 是真正合法 YAML。
现在 GitHub raw 显示它基本被压成一两行，虽然可能只是显示问题，但请在本地用 yaml.safe_load 和 load_haptic_plan_config 实际验证。

E. 跑测试：

pytest tests/test_haptic_plan_config_validation.py tests/test_haptic_trial_scheduler.py tests/test_haptic_digit_onset_guard.py tests/test_simple_haptic_sender_disabled.py tests/test_scheduler_sender_dry_run.py