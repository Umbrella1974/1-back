我想重新检查并设计当前项目里的 participant calibration reuse / quick check 流程。请先阅读现有代码，弄清楚当前完整 calibration、calibration save/load、quick check、failure reason、recalibration trigger 分别是怎么实现的，然后先和我讨论设计，不要立即大规模修改代码。

## 背景

当前实验需要 participant-specific calibration，主要包括：

* Finger states:

  * Open
  * C-shape / Contact posture
  * Pinch
* Wrist:

  * Neutral
  * Left / Right / Up / Down calibration

目前已经实现 calibration reuse 和 quick check，但实际 pilot 中发现一个严重问题：

> quick check 几乎每次都会失败，随后触发 full recalibration。

结果是 calibration reuse 基本失去意义，而且完整 calibration 有时甚至比正式实验 session 本身还耗时。

我怀疑当前 quick check 过于强调：

> 当前姿态的绝对数值是否接近上一次 calibration reference

而不是：

> 上一次建立的 participant-specific action space 今天是否仍然足够有效，可以继续用于分类。

另外，一个 pilot participant 暴露了一个重要问题：

* 她在单次 calibration 内可以非常稳定地保持 C-shape；
* 但不同 session 中，她自己选择的 C-shape closure 程度变化非常大；
* 有时 C 很靠近 Open；
* 有时 C 已经非常靠近 Pinch；
* 如果每次 quick check fail 后立刻重新 full calibration，程序实际上会不断“追着 participant 当天的动作变化重新定义动作空间”，而不是维持一个相对稳定的 participant reference。

因此我希望重新思考 calibration 和 quick check 的职责。

---

# 我目前倾向的新设计

## 1. Full calibration 的目标需要改变

第一次完整 calibration 不应该只获得：

> 每个动作做一次 → 得到一个精确 reference value。

而应该用于估计：

> participant 正常执行这些动作时的典型位置 + 自然重复性 / variability。

例如 Finger calibration 可以考虑让 participant 做多次完整循环：

Open → C → Pinch → C → Open

建议先讨论：

* 做 3 个 cycle 是否合理；
* 每个稳定状态需要采多少秒；
* 怎样自动提取每一次 repetition 的稳定窗口。

希望最后保存的不只是一个 mean/reference，而包括类似：

* open_median
* open_MAD / variability
* contact_median
* contact_MAD / variability
* pinch_median
* pinch_MAD / variability

以及每次 repetition 的 summary，而不是只保存最终平均值。

还应保存：

* open_contact_gap
* contact_pinch_gap
* normalized_contact_position
* repetition-to-repetition consistency

这里最重要的是能检测：

> participant 每一次 C 都很稳定，但三次 C 的位置互相差很大。

这种情况不应该被认为是一个高质量 calibration。

---

## 2. C-shape 应使用相对 Open–Pinch action space 来检查

希望考虑把 finger closure 归一化到 participant 自己的活动范围：

Open = 0% closure
Pinch = 100% closure

然后记录 C-shape 在这个范围里的 normalized position。

例如：

C repetitions:
52%
57%
54%

说明 participant 有相对稳定的 C posture。

而：

55%
84%
61%

即使每一次内部 variance 很低，也说明 participant 对 C-shape 没有稳定重复。

这里暂时不要替我硬编码类似“C 必须在 30–70%”这种 arbitrary threshold。

请先：

1. 看当前代码里的 distance 定义；
2. 判断归一化应如何实现；
3. 建议应该记录哪些统计量；
4. 哪些阈值可以先作为 configurable candidate；
5. 哪些阈值应该等多个 pilot 后再冻结。

---

# 3. Quick check 的职责

Quick check 不应该重新估计一个新的 calibration。

它应该回答：

> 当前 participant / sensor / hand configuration 是否仍然与保存的 calibration 足够一致，使旧 calibration 仍然可用于正式分类？

也就是说：

Full calibration = establish participant action space

Quick check = validate whether that action space is still usable

而不是：

Quick check = 检查今天能否精确复制昨天的 reference。

---

# 4. 希望把 quick check 压缩成一个很短的连续动作

我不希望后续每个 session 又分别执行：

Open
C
Pinch
Wrist Left
Wrist Right
Wrist Up
Wrist Down
...

这样实际上又变成一次 calibration。

我目前考虑 quick check 使用一个短 finger sequence：

Open → C → Pinch → C → Open

再加：

Wrist Neutral

即一次连续 finger cycle + 1–2 s neutral。

希望整体大约十几秒量级，而不是数分钟。

请评估现有代码是否容易支持这种流程。

---

# 5. Finger quick check 应优先检查这些东西

请讨论是否合理，不要直接照抄实现。

第一层：Signal quality

例如：

* tracking valid
* sufficient valid samples
* no extreme instability/noise
* reasonable sample continuity

第二层：State geometry

希望重点检查：

Open > C > Pinch

同时：

* Open–C separation 仍然足够
* C–Pinch separation 仍然足够
* C 没有过分靠近 Open
* C 没有过分靠近 Pinch

这里应更关注：

> separability / relative geometry

而不是单纯：

> current_open - old_open < X mm

例如：

Old calibration:
Open = 0.140
C = 0.070
Pinch = 0.020

Today:
Open = 0.132
C = 0.061
Pinch = 0.018

虽然三个绝对值都有漂移，但 action structure 仍然很清楚。

这种情况原则上可能应该 PASS。

反过来：

Open = 0.130
C = 0.031
Pinch = 0.027

虽然 ordering 还成立，但 C 几乎等于 Pinch，这种情况应该 FAIL。

请查看当前代码并建议怎样把这种逻辑映射到现有 calibration model。

---

# 6. Quick check tolerance 应利用第一次 calibration 测到的 participant variability

我不想简单地：

> 把当前 tolerance 从 5 mm 调成 10 mm / 20 mm。

更希望第一次 full calibration 通过 multiple repetitions 得到 participant-specific variability。

然后后续 quick check 的 tolerance 可以参考：

* calibration MAD
* inter-repetition variability
* state separation margin

即：

> tolerance 来源于这个 participant 自己正常动作的波动范围，

而不是完全 arbitrary 的统一 absolute threshold。

请评估现有数据结构是否支持这一点，以及推荐一个简单、不过度复杂的 candidate rule。

不要一开始引入复杂 machine learning / gesture classification。

---

# 7. Wrist quick check 希望大幅简化

当前我不希望每个 session quick check 时重新做 Left / Right / Up / Down。

完整 wrist calibration 只在 full calibration 时做。

后续 quick check 只检查：

> Wrist Neutral

希望检查：

* tracking valid
* neutral stable
* current neutral 是否仍处于 participant 原来的 neutral-centered acceptable region

注意之前已经发现旧 wrist classifier / neutral gate 有一个重要问题：

旧算法曾经根据 Left/Right 或 Up/Down calibration means 的中点定义 neutral region。

如果左右或上下动作幅度不对称，这会导致真正的 neutral=0 被排除在 neutral region 之外。

因此 quick check 不应该继续依赖这种 midpoint-centered neutral。

请检查当前代码现在到底使用什么逻辑，不要假设它已经修复。

我倾向：

> neutral-centered reference + participant-specific noise/stability tolerance

但请先根据现有代码分析。

---

# 8. Quick check 第一次失败后不要立即 full recalibration

这是这次 redesign 最重要的行为变化之一。

当前倾向流程：

Load saved calibration
↓
Quick check attempt 1
↓
PASS → reuse calibration

如果 FAIL：

不要立即 full recalibration。

先告诉 participant 哪个动作不符合要求，例如：

* Please fully open your hand.
* Please form the standard C-shape without pinching.
* Please pinch fully.
* Please relax your wrist to neutral.

然后：

Quick check attempt 2

如果 attempt 2 PASS：

> reuse existing calibration

如果 attempt 2 仍然 FAIL：

> 才进入 recalibration decision

原因：

有些 quick check fail 只是 participant 第一次姿态没有摆标准，而不是 calibration 真失效。

如果第一次 fail 后立即重新 calibration，会把一次异常动作正式写成新的 reference。

---

# 9. 第一版不要过度扩展 targeted recalibration

长期来看可以考虑：

如果：

Open pass
C fail
Pinch pass
Wrist pass

只重新采 C reference，而不是全部重标。

但是当前 milestone 我不希望一次改太多。

第一版可以仍然：

> fail twice → full recalibration

但请在设计里保留 failure-by-component 的结构，例如：

* open_failed
* contact_failed
* pinch_failed
* wrist_neutral_failed
* signal_quality_failed
* insufficient_state_separation
* contact_position_out_of_range
* etc.

以后再决定是否做 targeted recalibration。

---

# 10. Calibration QC 还要检查 repetition consistency

新的 full calibration 最重要的新功能之一，是区分：

A. within-hold stability

例如 participant 保持一个 C posture 2 s 非常稳定。

和：

B. between-repetition reproducibility

例如 participant 三次 C 分别：

0.080
0.043
0.076

每次内部都非常稳定，但三次之间差异巨大。

这种 calibration 不能只因为 MAD 很小就 pass。

请提出一个简单、可解释的 repetition consistency metric。

可以考虑：

* range
* SD/MAD across repetition medians
* normalized spread relative to Open–Pinch range

但请先比较哪种最适合现有数据。

---

# 11. Formal detector 与 quick check 要保持边界

这次修改不要顺便：

* 修改 first-excursion detector
* 修改 response RT algorithm
* 修改 tactile scheduler
* 修改 single/dual task structure
* 修改 semantic mastery
* 修改 1-back
* 修改正式 Open/C/Pinch outcome scoring

本 milestone 只处理：

> calibration acquisition quality
> calibration save/load
> calibration reuse
> quick check
> recalibration trigger

如果当前代码中这些模块耦合，请指出，但不要顺手重构整个实验。

---

# 12. Backward compatibility

已有 pilot 数据和 calibration bundle 可能只保存：

* old min/max
* old threshold_a
* older wrist calibration information
* single-reference Open/Pinch
* 部分版本可能已经有 C reference，但没有 repetition-level data

请检查当前 calibration file schema。

新的 calibration format 应考虑：

* calibration_version
* repetition-level summaries
* old fields retained where necessary
* old calibration bundle 是否还能 load
* 如果旧 bundle 缺少新 QC fields，应该：

  * fallback 到 legacy quick check？
  * 强制一次新的 full calibration？
  * 还是允许 partial compatibility？

请先提出方案，不要擅自删除旧兼容路径。

---

# 13. Logging

希望 quick check 至少记录：

* calibration_id / calibration_path
* calibration_version
* calibration_loaded
* quick_check_attempt
* overall pass/fail
* pass/fail by component
* failure_reason(s)
* current Open/C/Pinch summary
* normalized C position
* state separation values
* wrist neutral summary
* whether guided retry occurred
* whether reuse succeeded
* whether full recalibration was triggered

如果当前 summary/log 已经有部分字段，请尽量扩展而不是重复造一套日志。

---

# 14. 我希望你先做的事情

现在先不要实现。

请先：

1. 找出当前项目中所有和：

   * finger calibration
   * wrist calibration
   * calibration save/load
   * calibration reuse
   * quick check
   * quick-check failure
   * full recalibration
     相关的代码文件和主要函数。

2. 用现有代码解释当前流程：

   * full calibration 怎么得到 reference
   * quick check 实际检查什么
   * 当前 tolerance 是什么
   * 哪些 failure reason 会直接触发 recalibration
   * wrist neutral 当前到底如何判定
   * calibration bundle 当前保存哪些字段

3. 对比我上面提出的新逻辑，指出：

   * 哪些已有功能可以直接复用
   * 哪些地方需要改
   * 有没有我对当前实现理解错误的地方
   * 有没有明显风险

4. 给我一个**最小 milestone proposal**。

我希望第一版尽量只实现：

* full calibration multiple repetitions
* repetition consistency QC
* 保存 participant-specific variability
* short quick check
* relative state geometry / separability check
* wrist neutral-only quick check
* guided retry once
* fail twice 才 full recalibration
* logging

暂时不要做：

* targeted partial recalibration
* automatic calibration adaptation
* moving-average reference update
* machine learning classifier
* per-session automatic threshold optimization

5. 在我确认设计以后再写代码。

---

# 最核心的设计原则

请始终围绕这一句话判断：

> Full calibration should establish a participant-specific but behaviorally stable action space; quick check should verify whether that previously established action space remains usable, rather than redefining the action space every session.

另外：

> Participant-specific numeric parameters are acceptable, but the calibration system should not silently absorb large changes in the behavioral meaning of Open, C-shape, Pinch, or wrist responses.

请先基于实际代码和我讨论，不要直接开始大规模修改。
