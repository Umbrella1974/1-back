我基本同意。现在这套方案已经收敛了，不需要再反复改总体框架。接下来真正应该防的是“分析规则在看完数据之后越调越漂亮”。

Codex 现在这7步里，我只会再补三个约束。

第一，`calibration logging` 不必真的排到第6步才做。它和前面的离线分析可以并行。因为它只影响下一次 pilot 的日志，不影响当前数据分析。下一位受试者开始前就应加入，否则又会损失一轮标定分布信息。

第二，slip 和 wrist 的 detector 都要经历“pilot确定 → 冻结 → 正式实验使用”这个过程。比如现在：

```text
closure_delta >= max(0.05, 3 × baseline_MAD)
stable ≥ 100–150 ms
```

这些可以在现有 pilot 上探索。但等再有2–3名 pilot 后，应确定一个版本，例如：

```text
response_detector_version = v1.0
```

然后正式实验期间不要因为某几个 trial 看起来不好又改阈值。否则后面统计上很难解释。

同样，`clean / recoverable / contaminated` 的定义也要在正式实验前冻结。不能正式数据出来以后再根据条件差异决定哪些 trial 算 contaminated。

第三，统一的 `cue_response_metrics.csv` 很重要，但不要为了“统一”强行让所有 cue 使用完全相同的行为字段语义。应该统一的是上层结构：

```text
event_name
response_source
expected_response

cue_onset_ms
response_onset_ms
response_rt_ms

first_response_correct
eventual_correct

response_end_ms
full_cycle_ms

trial_quality
quality_reason
```

而底层可以保留 modality-specific 指标。

例如 wrist cue额外有：

```text
first_wrist_direction
correct_wrist_direction
neutral_return_ms
lr_score_at_onset
ud_score_at_onset
```

slip额外有：

```text
baseline_closure
pinch_onset_ms
peak_closure
peak_closure_delta
release_onset_ms
release_drop
returned_to_precue_baseline
```

这样以后做总体统计时用统一字段，而做QC或解释异常时仍然有原始行为特征。不要把这些丰富信息为了统一CSV全部压掉。

关于 slip，我现在认为定义已经比较清楚了：

> cue onset
> → 相对于pre-cue baseline出现新的显著 pinch
> → 达到 peak
> → peak以后出现显著 reopening
> → slip response complete。

这里的 `full_cycle_ms` 我建议定义为：

> `release completion/onset − cue onset`

具体用 release onset 还是 release稳定完成时刻，要在算法实现时统一。为了跟 wrist 的“动作→回neutral”对应，我更倾向于用**release达到判定条件的时刻**，而不是刚开始松手的第一帧。这样 full cycle 真正表示一次完整 S-R 行为完成所需时间。

同时保留：

```text
pinch_rt_ms
```

作为“开始执行反应”的时间。

于是你实际上得到两个不同指标：

> `response_rt`：人多久开始正确反应；
> `full_cycle`：人多久完成完整动作。

这两个不要混。

对 wrist 也一样：

> cue → correct directional response onset = RT
> cue → 动作完成并稳定回neutral = full cycle。

这样以后Exp2的主要行为指标就会比较干净。

还有一个小细节：matrix slip 如果在 `haptic_events.csv` 里记录多个step，Codex说“只用第一步的 `actual_emit_ms`”是合理的，但最好不要靠诸如 `slip_matrix_step_2` 的字符串排除。更稳的是明确建立：

```text
semantic_event_id
parent_event_name
sequence_step
```

或者利用已有能唯一识别“semantic slip onset”的字段。因为以后matrix pattern一旦从2 step改成3 step，字符串规则容易坏掉。

至于 contact/release，现在留空完全没问题。等你明确告诉我这两个cue要求参与者做什么，再纳入统一 S-R mapping。不要为了让7种cue全部有RT而硬造行为指标。

所以现在我不会再让 Codex继续写新的“方案说明”了。下一步应该真正实现：

> wrist offline reclassification
> → slip offline detector
> → 把这位受试者12个有效session全部重新跑一遍
> → 看每种cue到底有多少 clean/recoverable/contaminated，以及新旧 wrist classifier 的差异。

这一步跑出来以后，我们才会知道这个 pilot 的数据究竟能恢复到什么程度，也能知道正式实验前还剩哪些真正的软件问题。现在概念层面已经足够了。
