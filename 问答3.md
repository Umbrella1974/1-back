整体方向是对的，而且现在看过你这位受试者真实的 `pinch_timeseries.csv` 后，我会更明确地说：**wrist 那条路线可以照这个顺序推进；slip 的分析思路也对，但“松开”的定义需要改。**

我直接对齐了这批数据里的 12 次 slip。结果很清楚：其中 **11/12 次在 cue 后都有明显的进一步捏合**。以 Codex 定义的归一化 `pinch_closure` 算，cue 前 baseline 大约在 0.40–0.67，而这11次进一步捏合的 closure 增量大约是 **0.22–0.55**；只有1次增量约 **0.017**，确实很像一次没有完成明显捏合反应的 trial。

所以这验证了一个重要前提：

> slip 的 S-R 完全可以从现有 `pinch_timeseries.csv` 离线恢复，而且“相对于 cue 前 baseline 检测一次新的捏合”比看 `closed_zone` 合理得多。

Codex 这里判断是对的。

但我不赞成它目前把“松开成功”定义成：

> `closure <= baseline_closure + tolerance`

并把“回到 cue 前 baseline”当作完整 slip response 的必要条件。

原因来自你的实际数据。很多 trial 是这种形态：

> baseline 0.43
> → pinch peak 0.98
> → release 后下降到 0.59

它**没有回到原来的0.43**，但从0.98下降到0.59已经是一个非常明显的松开动作。

我看了12次 slip，在那些有明显捏合的 trial 中，peak之后的 closure 回落幅度大约有 **0.25–0.67**。所以受试者实际上经常明确“捏了再松”，只是松完以后手指并不会机械地回到 cue 前完全相同的距离。

这和你刚才说的手腕问题其实是同一个道理：

> 人体动作不应该要求“回到一个精确数值”，而应该检测一次新的、有方向性的动作变化。

因此 slip 最好拆成两个概念：

> **pinch response**：相对于 cue 前 baseline 出现显著 closure increase；
> **release response**：在 pinch peak 后出现显著 closure decrease。

然后另外记录：

> `returned_to_precue_baseline = true/false`

作为一个附加指标，而不是决定 slip 是否正确完成。

例如第一版可以是：

```text
Pinch:
closure increase >= threshold
并持续 ≥ 100–150 ms

Release:
peak 后 closure decrease >= threshold
并持续 ≥ 100–150 ms

Baseline recovery:
是否进一步回到 pre-cue baseline ± tolerance
```

这样：

> 捏紧 → 明显松开但停在稍微不同的位置

仍然是一个正确的 `pinch_then_release`。

这更符合你的实际 S-R 指令。

---

还有第二个我会改的地方：**不要现在把 response window 硬封顶在5秒。**

尤其你前面已经告诉我，matrix slip 可能要3–4秒才能完整呈现。你当前这批 matrix 数据里，我看到 slip 的主 step 本身就是 `duration_ms=3000`，第二个 matrix step在其后100 ms发出。

而这位受试者真实数据中，明显 pinch 的：

> onset 大约出现在 cue 后 **1.1–1.9 s**；
> peak大约在 **1.45–2.6 s**。

所以捏合本身5秒够。

但是如果还要研究：

> pinch → release → settled

5秒就开始显得武断。尤其将来 matrix duration 真改成4秒以后更是如此。

你现在相邻 cue 本来间隔很长，所以离线分析第一版干脆采用：

> **当前 slip onset → 下一 semantic cue onset**

作为完整观察窗口。

然后另设：

> `response_deadline_ms`

作为QC，比如8秒，而不是直接在5秒把数据截掉。

等3–5个 pilot 出来以后，你自然就知道95%的 pinch/release cycle究竟需要多少时间，再定正式窗口。

---

第三点，Codex 的：

```text
closure_delta >= max(0.05, 3 * baseline_closure_mad)
```

作为**第一版诊断规则可以用**，但不要现在写成正式算法。

因为我看这位受试者实际数据时，cue前500 ms的 `baseline_closure_mad` 很小，很多接近0。因此这批数据中实际上几乎总是：

> `0.05` 在决定 threshold，

而不是 `3 × MAD`。

这不代表0.05不好。相反，从这位受试者的数据看，它非常容易区分：

> 正常 slip：Δclosure ≈ 0.22–0.55
> 那一次可疑 slip：Δclosure ≈ 0.017

所以0.05作为 pilot detector 有很大余量。

但以后最好结合你准备新增的 calibration distribution，比如：

> neutral/closed resting noise；
> participant-specific pinch variability；
> pre-cue signal range/SD。

最终再决定阈值，而不是把0.05写死成论文方法。

---

因此 slip 这部分，我会让 Codex把最终输出稍微改成这样：

```text
cue_onset_ms

baseline_closure
baseline_stability

pinch_onset_ms
pinch_rt_ms
peak_closure
peak_closure_delta
peak_time_ms
pinch_detected

release_onset_ms
release_drop
release_detected

return_to_precue_baseline_ms
returned_to_precue_baseline

full_cycle_ms

quality_label
quality_reason
```

其中：

> `pinch_detected + release_detected`

才对应你的 S-R：

> **target finger 与拇指捏合，然后松开。**

`returned_to_precue_baseline` 不决定对错，只描述动作最后有没有回到原来的手指间距。

---

至于它列出来的其他7项工作，我总体赞成，但顺序上我仍然会稍微调整一下。

现在真正应该先做的是：

1. wrist neutral离线诊断；
2. neutral-centered classifier作为候选算法；
3. wrist response离线重建；
4. slip的 `pinch → release` 离线重建；
5. 把两套结果合并成统一 event-level S-R analysis；
6. 同时尽快增加 calibration logging；
7. 最后才改 online wrist gate。

这里第5步其实很重要。你最终不要有：

> 一个 wrist 分析脚本；
> 一个 slip 分析脚本；
> 一个 contact分析脚本……

最后各算各的。

最好最终生成一个统一的：

```text
cue_response_metrics.csv
```

每个 semantic cue 一行：

```text
session
condition
event_name
response_source
expected_response

cue_onset
response_onset
response_rt

first_response
first_response_correct

correction
eventual_correct

return_or_release_onset
full_cycle

trial_quality
quality_reason
```

其中：

```text
left/right/up/down → wrist
slip → pinch
```

至于 contact、release，你还没有在这一条里告诉我它们对应的具体 S-R 行为，所以分析脚本里先留空，不应该让程序自己猜。

---

另外，关于 wrist，我仍然赞成：

> 不要立刻替换 live classifier。

因为我们现在通过这一个 participant 已经发现 old neutral classifier 确实存在系统性问题，但 neutral-centered midpoint 目前仍然应该被视为一个**候选修正方案**。先拿完整12组数据重新分类，看看：

> 原27个 gate failures有多少被合理恢复；
> cue前轨迹是否确实稳定；
> 新算法有没有反过来把明显动作误判为neutral。

确认以后再进 live code。

而 calibration logging 我会比 Codex排得更靠前。**下一次有人做 pilot 时就应该开始保存完整一点的标定分布。** 这只是增加日志，不改变实时行为，风险很低，但以后会非常有价值。

---

所以我现在对这套方案的结论是：

**整体执行框架可以采用。**

真正需要告诉 Codex修改的核心只有两点：

> slip 的“release”检测改为 **peak之后显著 reopening**，不要强制要求回到 pre-cue baseline；

以及：

> slip完整分析窗口第一阶段不要硬截5秒，先观察到 next cue，再从 pilot 数据确定合理deadline。

这两点不是理论上的挑刺，而是我刚刚直接看你的真实 slip 数据以后得到的。尤其第一点，如果按 Codex当前“必须回到baseline”来做，会把这位受试者一些肉眼从连续数据上看明显已经“捏紧再松开”的 trial 错标成 `partial_no_release`。这个应该在写脚本之前改掉。
