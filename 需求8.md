请直接修复，不要再问确认。

需要做：

1. 在 pinch_calibration.py 或 run_pinch_haptic_1back.py 中增加 calibration quality check。

config 增加：

calibration:
  open_hand_duration_s: 3.0
  pinch_hand_duration_s: 3.0
  threshold_ratio: 0.65
  min_valid_frames: 30
  min_distance_range: 0.02
  min_distance_range_ratio: 0.15

规则：
- distance_range = max_distance - min_distance
- 如果 distance_range < min_distance_range，则 calibration failed
- 如果 distance_range / max_distance < min_distance_range_ratio，也 calibration failed
- failed 时不要进入正式 1-back
- 屏幕和控制台明确提示：
  Calibration failed: max-min too small.
  Check target_finger_node_id, hand gesture, and whether you are opening/pinching the configured fingers.

2. calibration.json 和 summary.json 增加：
- distance_range
- distance_range_ratio
- calibration_passed
- calibration_failure_reason

3. 在正式运行 summary.json 增加 zone run 统计：
- max_open_zone_duration_ms
- max_closed_zone_duration_ms
- open_zone_run_count
- closed_zone_run_count

4. 如果 total_haptic_events=0，要在 summary warnings 里写明确原因候选：
- no_haptic_events
- max_open_zone_duration_ms
- min_contact_onset_delay_ms
- 如果 max_open_zone_duration_ms < min_contact_onset_delay_ms，则 warning：
  open_zone segments were shorter than contact onset delay; contact could not trigger.

5. 增加 debug/test config 选项：

haptic_debug:
  print_zone_transitions: false
  print_scheduler_events: true

调试时可以打印：
- enter open_zone
- pending contact sampled delay
- pending contact canceled because zone exited
- contact emitted
- enter closed_zone
- event emitted

6. 保留正式 plan 的随机 delay 不变。
但新增一个临时调试 plan，例如 haptic_plan_config_fast_debug.yaml：

timing:
  contact_onset_delay_ms: [0, 100]
  inter_event_gap_ms: [50, 100]
  refractory_ms: 1000

这个只用于确认 haptic pipeline，不用于正式实验。

7. 新增测试：
- calibration max-min 太小时会失败
- calibration failed 时不会进入正式 dual-task
- summary 会记录 calibration_failure_reason
- open_zone 最长持续时间小于 contact delay 时，summary warnings 给出原因