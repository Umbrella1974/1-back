下一步不要接 1-back 主循环，不要接真实 ESP32 TCP，不要写 run_pinch_haptic_1back.py。

先修复 haptic_plan_config.py 和 haptic_trial_scheduler.py 的 schema 不一致问题。

当前 haptic_trial_scheduler.py 已经依赖：
- plan.timing.contact_onset_delay_ms
- plan.timing.inter_event_gap_ms
- plan.timing.refractory_ms
- event.onset_delay_ms
- event.onset_gap_after_previous_ms

但是 haptic_plan_config.py 目前还没有这些字段。
请先完成：

1. 在 haptic_plan_config.py 中新增 HapticPlanTiming dataclass：
   - contact_onset_delay_ms: tuple[int, int]
   - inter_event_gap_ms: tuple[int, int]
   - refractory_ms: int

2. HapticPlanConfig 增加 timing 字段。

3. HapticPlanEvent 增加：
   - onset_delay_ms: tuple[int, int] | None
   - onset_gap_after_previous_ms: tuple[int, int] | None

4. haptic_plan_config_from_dict 允许顶层 timing。
   如果缺少 timing，可以给出明确报错，或者使用安全默认值；优先明确报错。

5. events[] 允许 onset_delay_ms 和 onset_gap_after_previous_ms。
   - contact 可以配置 onset_delay_ms
   - slip/left/right/release 可以配置 onset_gap_after_previous_ms
   - 如果 event 没有配置，则 scheduler 使用 timing 默认值

6. 增加 range 校验：
   - 必须是长度为 2 的 list/tuple
   - 两个值必须是非负整数
   - lower <= upper

7. 更新 to_dict()，确保 timing、onset_delay_ms、onset_gap_after_previous_ms 能 round-trip。

8. 新增 haptic_plan_config_example.yaml，至少包含：
   - timing
   - contact
   - slip
   - left
   - right
   - release
   - open_zone
   - closed_zone

9. 更新 tests/test_haptic_plan_config_validation.py，增加：
   - timing 能解析
   - event onset_delay_ms 能解析
   - event onset_gap_after_previous_ms 能解析
   - 非法 range 会报错
   - to_dict 后再 parse 结果一致

10. 跑：
   pytest tests/test_haptic_plan_config_validation.py tests/test_haptic_trial_scheduler.py tests/test_haptic_digit_onset_guard.py

不要修改 nback_task_final.py。
不要实现真实 TCP。
不要接 Pygame。