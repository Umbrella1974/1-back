现在只做 wrist rotation 集成收尾修复，不要重构，不要改 haptic、pinch、nback 主逻辑。

当前状态：
1. wrist_rotation.py 已经存在。
2. run_pinch_haptic_1back.py 已经在 wrist_rotation_config.enabled=true 时执行 wrist neutral/left/right calibration。
3. 正式阶段已经调用 classify_wrist_rotation_frame() 并写 logger.write_wrist_rotation_sample()。
4. dualtask_logger.py 已经有 wrist_rotation_calibration.json 和 wrist_rotation_timeseries.csv 写入方法。

现在只修以下问题：

问题 1：dualtask_config.yaml 缺少 wrist_rotation 配置段。
请增加：

wrist_rotation:
  enabled: false
  node_id: 0
  quaternion_order: wxyz
  calibration_duration_s: 3.0
  min_valid_frames: 30
  feature_method: calibrated_axis_projection
  classification_margin: 0.15
  save_timeseries: true
  required: false

默认 enabled=false，保证现有实验不变。

问题 2：dualtask_config.yaml 缺少 post-release 配置。
在 session 下增加：

post_release_recording_ms: 3000
post_release_continue_nback: false

问题 3：WristRotationConfig 缺少 required 字段。
在 wrist_rotation.py 的 WristRotationConfig 增加：

required: bool = False

wrist_rotation_config_from_dict() 读取 required。

run_pinch_haptic_1back.py 中：
- 如果 wrist_rotation.enabled=true 且 calibration failed：
  - required=false：写 warning，继续正式实验，但 wrist sample class 应为 unknown 或 invalid。
  - required=true：明确 raise RuntimeError，停止正式实验。

问题 4：wrist_rotation_timeseries.csv 字段缺少 distance_to_left / distance_to_right。
dualtask_logger.py 的 WRIST_ROTATION_TIMESERIES_FIELDS 增加：
- distance_to_left
- distance_to_right

不要改 WristRotationSample 的字段名。

问题 5：summary.json 增加更完整的 wrist 信息。
至少增加：

wrist_rotation_enabled
wrist_rotation_required
wrist_rotation_calibration_passed
wrist_rotation_failure_reason
wrist_rotation_calibration_json
wrist_rotation_timeseries_csv
wrist_rotation_save_timeseries

如果方便，再增加：
wrist_rotation_valid_samples
wrist_rotation_invalid_samples

问题 6：enabled=false 时不要创建空 wrist csv。
如果 wrist_rotation.enabled=false，不主动创建 wrist_rotation_timeseries.csv。
output_files 里可以保留路径，但文件不一定存在。

测试要求：
1. wrist_rotation.enabled=false 时，程序不执行 wrist calibration，不写 wrist timeseries，不影响已有流程。
2. wrist_rotation.enabled=true + calibration passed 时：
   - 写 wrist_rotation_calibration.json
   - 写 wrist_rotation_timeseries.csv
   - CSV 包含 distance_to_left 和 distance_to_right 字段。
3. wrist_rotation.enabled=true + calibration failed + required=false 时：
   - 不崩溃
   - summary 有 wrist_rotation_calibration_passed=false 和 failure_reason
4. wrist_rotation.enabled=true + calibration failed + required=true 时：
   - 抛出明确 RuntimeError。
5. py_compile 通过：
   python -m py_compile wrist_rotation.py run_pinch_haptic_1back.py dualtask_logger.py
6. pytest 通过：
   python -m pytest tests/test_wrist_rotation.py tests/test_pinch_haptic_1back_core.py

不要修改：
- exp2
- manus_vive_com
- MANUS TCP 8888
- haptic scheduler
- SimpleHapticSender
- nback_timeline
- pinch calibration