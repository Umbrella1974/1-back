当前不要进入 1-back 双任务主循环集成，不要接 Pygame，不要接真实 ESP32 TCP。

先修复当前仓库的可运行性问题。

我在 GitHub main 上看到这些问题：
1. run_pinch_haptic_dry_run.py import 了 dualtask_logger，但 dualtask_logger.py 在仓库 raw 路径下读不到。
2. dualtask_config.yaml 在仓库 raw 路径下也读不到。
3. haptic_plan_config_example.yaml 看起来被压缩成一两行，不像合法 YAML。
4. run_pinch_haptic_dry_run.py / tests 文件 raw 显示也像是被压缩成极少数长行，请确认本地实际文件是否有正常换行。如果没有，必须修复格式，否则 Python 会语法错误。

请先完成：

A. 确认并提交这些文件：
- dualtask_logger.py
- dualtask_config.yaml
- run_pinch_haptic_dry_run.py
- haptic_plan_config_example.yaml
- tests/test_dualtask_logger.py
- tests/test_pinch_haptic_dry_run_core.py

B. 确保所有 .py 文件有正常 Python 换行和语法。
运行：
python -m py_compile run_pinch_haptic_dry_run.py dualtask_logger.py haptic_plan_config.py haptic_trial_scheduler.py simple_haptic_sender.py manus_pinch_input.py pinch_calibration.py

C. 确保 haptic_plan_config_example.yaml 是合法 YAML，格式类似：

plan_id: plan_001
description: Contact first, then slip-left-right, release last.
random_seed: 123

timing:
  contact_onset_delay_ms: [500, 2000]
  inter_event_gap_ms: [300, 1000]
  refractory_ms: 3000

events:
  - name: contact
    modality: vibration
    command_label: contact_enter
    command_id: 1
    duration_ms: 150
    trigger_zone: open_zone
    onset_delay_ms: [500, 2000]

  - name: slip
    modality: vibration
    command_label: slip_start
    command_id: 3
    duration_ms: 800
    trigger_zone: closed_zone
    onset_gap_after_previous_ms: [300, 1000]

  - name: left
    modality: matrix
    channel_list: [1, 2, 3]
    duration_ms: 800
    trigger_zone: closed_zone
    onset_gap_after_previous_ms: [300, 1000]

  - name: right
    modality: matrix
    channel_list: [5, 6, 7]
    duration_ms: 800
    trigger_zone: closed_zone
    onset_gap_after_previous_ms: [300, 1000]

  - name: release
    modality: vibration
    command_label: contact_exit
    command_id: 2
    duration_ms: 150
    trigger_zone: closed_zone
    onset_gap_after_previous_ms: [300, 1000]

zones:
  open_zone:
    lower: auto_a
    upper: auto_max
  closed_zone:
    lower: auto_min
    upper: auto_a

D. 新增或修复 dualtask_config.yaml，至少包含：
session、manus、pinch、calibration、haptic、sync 六部分。

E. 运行测试：
pytest tests/test_haptic_plan_config_validation.py tests/test_haptic_trial_scheduler.py tests/test_haptic_digit_onset_guard.py tests/test_simple_haptic_sender_disabled.py tests/test_scheduler_sender_dry_run.py tests/test_dualtask_logger.py tests/test_pinch_haptic_dry_run_core.py

F. 如果本地测试通过，请把完整文件推到 GitHub 后，再确认 GitHub raw 能读到：
- https://raw.githubusercontent.com/Umbrella1974/1-back/main/dualtask_logger.py
- https://raw.githubusercontent.com/Umbrella1974/1-back/main/dualtask_config.yaml