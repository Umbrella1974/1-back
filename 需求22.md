标定的程序，虽然每次都可以用之前的，但是10次里9次都得重新标，看看为什么容错率这么低

容错率低的直接原因
复用时的"快速校验"（quick check）用这个公式算允许误差，见 run_pinch_haptic_1back.py:1334：


tolerance = reference_mad * open_mad_multiplier   # open_mad_multiplier 默认 6.0
delta = |current_median - reference_median|
if delta > tolerance:       → "open_distance_shifted_from_reference"
elif current_mad > tolerance: → "open_distance_unstable"
reference_mad 是上次标定时那 3 秒里手掌张开的抖动幅度（MAD，中位数绝对偏差），不是"跨次之间的自然漂移"。

数据说话（28 个标定文件的统计）
标定文件里 open_distance_mad 几乎全是亚毫米级：

标定文件	open 中位数	MAD	允许误差 = MAD×6
v13	146.6 mm	0.071 mm	0.43 mm
v05	121.2 mm	0.037 mm	0.22 mm
v04	131.3 mm	0.070 mm	0.42 mm
v14	150.0 mm	0.233 mm	1.40 mm
而同一个人跨次之间的 open 中位数漂移有多大？看 12324075_exp2 这一串：

v05 = 121.2 mm → v09 = 155.8 mm，差了 34.6 mm。
也就是说：允许误差 0.2~1.4 mm，但真实跨次漂移 10~34 mm，差了 一到两个数量级。所以几乎每次一戴上手套（手套戴的位置不同、手指张开角度不同），快速校验就报 open_distance_shifted_from_reference，触发完整重标。这就是"10 次里 9 次得重标"。

为什么会这样设计错
MAD 衡量的是"标定那 3 秒你手抖不抖"——人保持静止时是亚毫米级，所以 MAD×6 依然只有 1mm 左右。
乘数 6.0 看起来很大，但乘在一个接近 0 的基数上毫无意义。
真正的跨次变异来源（手套重新佩戴、手指张开程度、追踪系统偏移）根本没被纳入容差。
还有个隐藏问题：检查"错位"
快速校验只检查张开（open）距离和手腕，不检查捏合（pinch）距离。但分类真正敏感的边界是 threshold_a（open/closed 分界，约在 106mm 处），离 open 的 ~150mm 很远。也就是说：

它对 open 距离卡得极严（1mm 级），而 open 距离其实离阈值很远、稍微漂一点根本不影响分类；
它又完全没校验真正影响 closed_zone 的 pinch 距离。
两头都反了。

建议修法
把允许误差改为"按标定距离范围的比例 + 一个绝对下限"来算，而不是按 MAD：


tolerance = max(
    reference_mad * open_mad_multiplier,      # 保留原有稳态判定
    calibration.distance_range * 0.05,        # 按量程给容差，125mm 量程 → ~6.3mm
)
distance_range 是 max_distance - min_distance（v14 是 125mm），PinchCalibrationResult 里已经存了这个字段。这样容差约 6mm，足够吸收手套重戴的自然漂移，同时相对 125mm 量程只占 5%，不会实质影响分类边界。

要我直接把这个改上吗？如果要，我会顺便加一个 min_distance_range_ratio 类似的配置项到 CalibrationReuseConfig，让你能在 YAML 里调这个 0.05 的比例，而不是写死。