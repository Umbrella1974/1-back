你的理解正确。请不要先改大逻辑，先确认当前代码是否已经完整支持 post_release_continue_nback=true。

请检查 run_pinch_haptic_1back.py 中 release 后 post-release 阶段的代码路径：

1. post_release_continue_nback=true 时，post-release 3 秒内是否继续：
   - nback_timeline.tick(now_ms)
   - nback_timeline.finalize_until(now_ms)
   - pygame key / keyboard response polling
   - logger.write_nback_event 或等价 nback event logging

2. post_release_continue_nback=false 时，保持旧行为：
   - post-release 继续 MANUS/pinch/wrist recording
   - 但不再出新 1-back 数字
   - 不再接收新的 1-back response

3. release 后 post-release 阶段无论 true/false，都不能再触发新的 haptic event。

4. 到 post_release_end_ms 后，统一结束 session，end_reason 仍为：
   haptic_release_post_recording_complete

如果当前代码已经支持，请只改 dualtask_config.yaml：
session.post_release_continue_nback: true

如果代码没有完整支持，请做最小修复，不要改 haptic scheduler、pinch calibration、wrist_rotation、MANUS TCP。

新增/修改测试：
- post_release_continue_nback=false：release 后 nback trial 数不再增加。
- post_release_continue_nback=true：release 后 3 秒内 nback trial 数继续增加。
- 两种情况下 pinch_timeseries 都继续记录到 post_release_end_ms。
- 两种情况下 haptic 都不再触发新事件。
- end_reason 都是 haptic_release_post_recording_complete。

运行：
python -m py_compile run_pinch_haptic_1back.py
python -m pytest tests\test_single_haptic_episode_end_policy.py tests\test_pinch_haptic_1back_core.py
python -m pytest