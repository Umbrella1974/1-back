只做 wrist rotation 集成收尾，不要改 haptic、pinch、nback、MANUS TCP。

修复：
1. dualtask_logger.py 的 WRIST_ROTATION_TIMESERIES_FIELDS 增加 distance_to_left 和 distance_to_right。
2. WristRotationConfig 增加 required: bool = False，并从 dualtask_config.yaml 读取。
3. run_pinch_haptic_1back.py 中，如果 wrist_rotation.enabled=true 且 calibration failed：
   - required=false：写 warning，继续；
   - required=true：raise RuntimeError，停止正式实验。
4. summary.json 增加：
   wrist_rotation_required
   wrist_rotation_failure_reason
   wrist_rotation_calibration_json
   wrist_rotation_timeseries_csv
   wrist_rotation_save_timeseries
5. live formal result 的 session_should_end 要把 haptic_release_post_recording_complete 也算作正常 release end。
6. 测试 enabled=true/false、required=true/false、CSV 字段、summary 字段。

不要改：
exp2
manus_vive_com
MANUS TCP 8888
haptic scheduler
SimpleHapticSender
nback_timeline
pinch calibration