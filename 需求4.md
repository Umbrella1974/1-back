下一步只做 scheduler -> disabled sender 的 dry-run 闭环。
不要接 1-back 主循环。
不要接 Pygame。
不要接 MANUS live TCP。
不要实现真实 ESP32 TCP。
不要修改 nback_task_final.py。

目标：
把 haptic_trial_scheduler.py 输出的 ScheduledHapticEvent 记录到 SimpleHapticSender，并写出 haptic_events.csv。
这一步完成后，应当可以用一个纯 Python dry-run 测试，从 plan config 生成一轮或多轮 haptic events，并确认 CSV 中完整记录 contact/slip/left/right/release 的时间、随机 delay/gap、digit guard 调整结果。

具体修改：

1. 修改 simple_haptic_sender.py

在 HAPTIC_EVENT_FIELDS 中增加：
- sampled_delay_ms
- sampled_gap_ms

在 HapticEventRecord 中增加：
- sampled_delay_ms: int | None = None
- sampled_gap_ms: int | None = None

在 _record_event() 中增加参数：
- sampled_delay_ms
- sampled_gap_ms

并写入 HapticEventRecord。

2. 新增方法：

def record_scheduled_event(self, scheduled: ScheduledHapticEvent) -> HapticEventRecord:

它应该根据 scheduled.event_name 分发：
- contact -> vibration
- release -> vibration
- slip -> vibration
- left -> matrix
- right -> matrix

并完整传递这些字段：
- haptic_trial_index
- event_index
- command_label
- command_id
- channel_list
- duration_ms
- trigger_zone
- trigger_pinch_distance
- trigger_frame_index
- original_planned_onset_ms
- adjusted_onset_ms
- nearest_digit_onset_ms
- digit_onset_delta_ms
- onset_was_delayed
- sync_warning
- sampled_delay_ms
- sampled_gap_ms

3. 新增测试：
tests/test_scheduler_sender_dry_run.py

测试内容：
- 构造一个 plan：contact -> slip -> left -> right -> release。
- 用 HapticTrialScheduler 推进完整一轮。
- 对每个 ScheduledHapticEvent 调用 sender.record_scheduled_event(event)。
- 写出 haptic_events.csv。
- 断言 CSV 里事件顺序是：
  contact, slip, left, right, release
- 断言 left/right 的 channel_list 被正确写入。
- 断言 contact 行有 sampled_delay_ms。
- 断言 slip/left/right/release 行有 sampled_gap_ms。
- 断言 original_planned_onset_ms 和 adjusted_onset_ms 存在。
- 断言 disabled 模式下 tcp_enabled=False, send_status=disabled。
- 断言 session_id 正确。

4. 可选新增一个命令行 dry-run 脚本：
dry_run_haptic_plan.py

参数：
--plan haptic_plan_config_example.yaml
--out outputs/dry_run_haptic_events.csv

它不需要 MANUS，不需要 1-back，不需要 ESP32。
它只模拟 zone/time：
- open_zone 进入
- 等 contact delay
- contact
- closed_zone 进入
- 按 plan 跑完
- 写 haptic_events.csv

5. 跑测试：
pytest tests/test_haptic_plan_config_validation.py tests/test_haptic_trial_scheduler.py tests/test_haptic_digit_onset_guard.py tests/test_simple_haptic_sender_disabled.py tests/test_scheduler_sender_dry_run.py