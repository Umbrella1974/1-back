下面这些内容最值得带到下一个对话，尤其是代码当前状态、实验设计决策、已知风险和待办。

```markdown
# 1-back / Exp2 当前项目状态摘要

工作目录：
`D:\research_history\first_one\research_code\1-back`

外部 ESP32 代码目录：
`D:\research_history\vscode\esp32_feedback_20250810\esp32_feedback_20250810`

## 当前运行入口

单次运行仍然用：

```bash
python run_pinch_haptic_1back.py --config only-matrix.yaml
python run_pinch_haptic_1back.py --config only-motor.yaml
python run_pinch_haptic_1back.py --config dualtask_config.yaml
```

manifest 多 session 运行用：

```bash
python run_participant_manifest.py --manifest participant_manifest_xxx.yaml
```

## 实验/模式命名

- `dual-task`：指同时做 1-back + tactile/haptic 任务。
- `single` / tactile-only：指只做 tactile/haptic，不做 1-back。
- `motor-only`：只用 vibration/motor ESP32。
- `matrix-only`：只用 HV507 matrix ESP32。
- `combined` / mixed：motor + matrix 混合触觉，不要再叫 dual，避免和 dual-task 混淆。

## 触觉 plan 设计

当前触觉顺序模板为：

| 文件 | P1 | P2 | P3 | P4 | P5 |
|---|---|---|---|---|---|
| 1 | S | F | R | L | L |
| 2 | B | R | L | F | B |
| 3 | F | B | S | R | F |
| 4 | R | S | B | S | R |
| 5 | S | L | F | B | S |
| 6 | L | L | R | F | B |

映射：
- `F = forward/up`
- `B = backward/down`
- `L = left`
- `R = right`
- `S = slip`

已有相关 plan：
- `haptic-plan-only-motor-1.yaml` 等 only-motor 系列
- `haptic-plan-only-matrix-1.yaml` 到 `haptic-plan-only-matrix-6.yaml`
- `haptic_plan_dual-1.yaml` 到 `haptic_plan_dual-6.yaml`
- `haptic_plan_combined-nozone-*.yaml`

## 当前通讯判断

### Motor/vibration ESP32

- vibration TCP 默认：
  - host: `192.168.1.22`
  - port: `12346`
- motor cue 当前大致映射曾确认过：
  - `contact` -> command id 1
  - `release` -> command id 4
  - `slip` 需要明确终止/或按当前 ESP32 协议处理
- motor 部分实际测试后基本可用。

### Matrix/HV507 ESP32

当前 PC 端 matrix 协议可对接：

`D:\research_history\vscode\esp32_feedback_20250810\esp32_feedback_20250810\esp32_fast_main_hv507_gate_autoff.py`

ESP32 端：
- port: `12345`
- packet 格式：

```text
AA 55 AA 55 + length + payload + checksum
```

PC 端：
`vendor_exp2_abc/matrix_haptic_protocol.py`

支持：
- `hold` -> control byte `0xE0`
- `auto_off` -> `0xE0 | duration_code`
- `off` -> `0x80`

注意：
- gate_autoff 固件不应该再依赖 legacy raw channel 包。
- matrix plan 需要显式 `matrix_output` 或 event/step `output`。
- `auto_off` duration 必须是 50ms 的正整数倍，且 <= 1550ms。
- `hold` 不会按照 event 的 `duration_ms` 自动关闭，只会保持到下一次 matrix 包、off 包、TCP 断开，或者程序显式发 off。
- 两个 matrix cue 同时发给同一个 ESP32 时，当前不是自动合并通道，而是后发包覆盖前发包。如果要同时出现，应写成同一个 event 的 `channel_list` 或同一个 `matrix_sequence` step。

## 当前已实现的重要功能

### 1. vendor_exp2_abc

在 `1-back` 中新增了 `vendor_exp2_abc`，包含 haptic TCP worker、matrix protocol 等。

### 2. MANUS-only pinch input

可以从无 tracker 的 combined JSON 中读取 `pinch_distance`。

### 3. Calibration

已有：
- open hand calibration
- pinch calibration
- 后续增加了 contact/C-shape reference
- wrist neutral/up/down/left/right calibration
- calibration timeseries 保存

标定相关风险：
- 曾发现 calibration 阶段可能混入 queue 中旧帧。
- 已实现阶段开始前 queue flush。
- 记录：
  - `queue_depth_before_flush`
  - `flushed_count`
  - `first_frame_index`
  - formal loop 中持续记录 `queue_depth`
  - `frame_age_ms`
- 正式 loop 暂时保持 FIFO，没有改成 latest/drop policy。
- README 里应保留警示：只有当正式运行中 queue_depth/frame_age_ms 显示持续 backlog，才考虑 latest/drop policy；RT 检测不应贸然丢帧。

### 4. Haptic plan parser

支持：
- plan id
- random seed/template seed
- event list
- `matrix_sequence`
- `matrix_output`
- `output: {mode: auto_off/hold/alternate}`
- `simultaneous_group`
- `cue_dispatch_mode`
- n-back trial gate/window
- wrist neutral soft guard

### 5. SimpleHapticSender

支持：
- disabled mode：只记录不发 TCP
- vibration TCP
- matrix TCP
- matrix sequence
- matrix auto_off/hold/off/alternate
- release 时可以发送 matrix off
- haptic event CSV 记录了更多字段

### 6. 两种 cue dispatch mode

当前支持两种模式：

#### `zone_sequential`

旧模式/默认模式。

特征：
- 每个 cue 单独按顺序发。
- 依赖 `trigger_zone`。
- contact/release/slip/up/down/left/right 等 cue 要根据 pinch zone、trial window、wrist neutral 等条件触发。
- 适合当前已经跑过的正式/半正式实验。

#### `timed_grouped`

新模式。

特征：
- 不依赖 pinch zone。
- 按时间 scheduler 触发。
- 可以通过 `simultaneous_group` 让多个 cue 在同一时间点发送。
- 同组内发送顺序：vibration 先 queue，再 matrix queue。
- 用于 combined/no-zone 这类新模式。
- 配置里可以删除 `trigger_zone`、`zones`、wrist guard 参数，避免误导。

运行仍然走原来的 Python 程序，只是 config/manifest 里指定不同 `cue_dispatch_mode`。

## Seed 设计

已做/计划过：
- session master seed
- 从 session seed 派生：
  - haptic seed
  - nback seed
- YAML template seed 保留用于审计，但 session seed 可覆盖实际运行 seed。
- 如果 manifest 中 `run_seed: null`，程序会随机生成实际 seed，并写入输出 CSV/summary，之后可用来复现。
- 如果 `run_seed` 填固定整数，例如 `111`，后续派生的 session_seed/haptic_seed/nback_seed 是确定不变的。

## Manifest 设计

一个 participant_id 下可以有多个 run_id。

目标：
- 支持多个 session 顺序运行。
- 支持 calibration 复用。
- 支持从某个 order/session 恢复继续，避免 TCP send failed 后整个受试者报废。
- 已讨论应增加启动 manifest 时选择 order 继续的功能。
- calibration reuse 需要注意：不要复用 `pinch_reference_quality_passed=False` 的旧 calibration。

## 1-back 相关

单独 1-back 中：
- 用户本地曾设置 `NUM_TRIALS = 51`，因为 50 时最后只有 49 个有结果。
- 原因：1-back 的第一个 trial 没有前一项可比较，所以第一个 trial 结果是 N/A。
- 所以如果想要 50 个“可评分结果”，需要 51 个刺激/trial。
- 如果说“50 trial”，严格说会有 49 个 1-back 可评分结果。
- 之前单独 1-back 中有休息导致 20、40 trial N/A，已计划/处理过“单独 1-back 关闭休息”的需求。

## Release 与 trial window

当前设计目标：
- release 出现在 40-50 trial 窗口。
- release 结束后继续补完 1-back，直到所有程序停止。
- 不希望 release 之后 3s 延迟期间 1-back 立刻停止。
- 前面的 haptic 尽量在 45 trial 前完成。
- 时间 gap 与 trial window 同时约束：
  - 时间 gap 决定 scheduler 本来什么时候想发。
  - trial window 只防止 cue 过早或异常过晚。

建议过的字段：
- `planned_onset_ms`
- `time_ready_ms`
- `trial_gate_open_trial`
- `actual_emit_ms`
- `emit_trial_number`
- `held_by_trial_gate`
- `delayed_by_digit_guard`
- `late_window_warning`

其中 `time_ready_ms` 特别重要，用来区分：
- scheduler 很早就 ready，只是 trial gate 等到后面才放行；
- scheduler 自己本来就到后面才轮到该 event。

## Wrist / 手腕动作

已有：
- left/right
- up/down
- neutral calibration
- first-excursion diagnostic

重要原则：
- 当前 first-excursion 主要用于 diagnostic，不要贸然改成正式 wrist detector。
- 后续可以扩展到所有方向，但不要每次分析都手写一遍。
- wrist neutral soft guard：
  - 等待回正；
  - 有 timeout；
  - timeout 后仍发；
  - 日志标记“不干净”。
- 需要根据真实 pilot 的 cue onset -> first valid response -> neutral return 分布决定 neutral timeout。

## Offline analysis

已有/讨论过：
- `analyze_cue_response_metrics.py`
- `analyze_cue_cycles.py`
- first-excursion diagnostic
- cue cycle metrics

重要原则：
- `nback_events.csv` 不参与 tactile response detection，只用于后续 dual-task performance 分析。
- tactile response detection 应该基于 haptic onset + wrist/pinch timeseries。

建议 cue_cycle_metrics.csv 带：
- `feedback_condition`
- `task_condition`
- `plan_id`
- `event_position`
- `emit_trial_number`
- `next_cue_onset_ms`
- `response_timeout`

### Contact/Release/Slip 离线定义

不要手动拍固定阈值。

第一版更干净：
- 保存 Open / C-shape / Pinch 三个 reference 的实际分布。
- 离线脚本计算连续量：
  - `distance_to_open`
  - `distance_to_contact`
  - `distance_to_pinch`
  - 相对于三个 reference 的归一化位置
- 暂时状态判定用三个 reference 的中点：

```text
OPEN ---- midpoint ---- C-SHAPE ---- midpoint ---- PINCH
```

前提：
`open_distance > contact_distance > pinch_distance`

如果 calibration/reference 严重重叠或顺序不满足，不硬评分三状态，直接标记 quality 不足。

定义：
- Contact: `OPEN -> C`
  - first_response_correct：是否第一有效运动方向是 closing
  - response_sequence_complete：是否最终进入 C-shape reference 区域并稳定
- Release: `C -> OPEN`
- Slip: `C -> PINCH -> C`
  - slip 最后不要求回到 cue 前完全相同的 pinch_distance，只要求回到 C-shape 合理参考范围

## 学习/测试程序

已规划/部分实现过一个 haptic 学习/测试程序：

目标：
- 命令行选择触觉模式：
  - dual/mixed/combined
  - only-motor
  - only-matrix
- 进入后按顺序播放所有 cue，让受试者学习。
- 顺序学习后，可以从列表中选择 cue 反复学习。
- 记录学习次数。
- 测试模式：
  - 选择触觉模式。
  - 该模式下每个 cue 出现 3 次。
  - 随机顺序。
  - 保留作答和 CSV。
  - 测试过程中不显示正确/错误反馈。
  - 测试结束显示总正确率和每个 cue 的正确率。
  - 如果某 cue 全错，明确提示需要重新学习该 cue。
  - learn/test 开始时允许填写 participant id。
  - 每次 learn/test 新建 CSV，不覆盖旧结果。

参考文件：
- `D:\research_history\first_one\research_code\input\input_function\interactive_experiment_send_to_esp32_motor_drv2605_tacton.py`
- `D:\research_history\first_one\research_code\input\input_function\interactive_experiment_send_to_esp32_hv507_gate_autoff.py`

## ESP32 handshake / send failed

出现过：
- haptic TCP send failed 后，重启 manifest 会从头开始。
- 用户希望能从指定 order/session 继续。
- 建议增加 handshake，并且 ESP32 也一起改，不要只在 PC 端假设连接成功。

用户要求：
- 不改原 ESP32 文件。
- 复制 `esp32-test-main-1.py` 新建一份 MicroPython 文件。
- 实现握手。
- 第一性原理、最小改动，不乱动其他逻辑。

注意：
- ESP32 matrix 当前 `esp32_fast_main_hv507_gate_autoff.py` 没有 ACK/handshake。
- PC 当前 matrix worker 也不读 ACK，这两者是匹配的。
- 如果未来加 handshake，需要 PC 和 ESP32 两边一起改。

## Camera 录像需求

已讨论：
- 可行：实验开始录像，结束停止，按 session/order 编号保存。
- 每个 order 最好单独录。
- 笔记本内置摄像头可行，负担通常可控。
- 手机也可行，但更复杂，可能需要作为网络摄像头或单独录像后同步。
- 如果加摄像头，建议第一版做可选配置，不影响主实验：
  - `video.enabled`
  - `camera_index`
  - `output_filename`
  - start/stop 时间写入 summary

## 当前需要警惕的问题

1. Matrix `hold` 可能持续太久，除非显式 off。
2. 两个 matrix 同时 cue 不会自动合并通道。
3. `matrix_latest_only: true` 可能导致 matrix 队列满时替换旧任务；正式实验前需确认这是否符合预期。
4. ESP32 matrix 第一次连接后有 7s 高压上电 sleep。TCP 连接可能已经建立，但 handler 延迟处理数据。正式实验中通常 calibration 会提供足够时间，但太早发送第一个 matrix cue 时要注意。
5. calibration reference quality 不足时，不要复用旧 calibration。
6. queue flush 已做，但正式 loop 是否 backlog 需要看 `queue_depth` 和 `frame_age_ms`。
7. single/tactile-only 没有 1-back，但 MANUS 判定逻辑理论上不受 1-back 影响。
8. dual-task 和 mixed/combined 命名要严格区分，避免实验记录混乱。
```