我认为后面的 manifest 部分有必要，而且对你的正式实验来说价值很高。只是它和 calibration reuse 的关系应该明确成：**A 是功能依赖，B 是实验可靠性工具。先把 A 做通，紧接着做 B，不需要等几轮 pilot 以后再做。**

你现在已经不是只有一两个 session 的程序了。后面会有 `feedback_type × task_type × 多个 plan`，还涉及 calibration、seed、participant ID。继续靠人工每次打开不同 YAML、确认 plan、确认 single/dual，很容易出现那种实验做完以后才发现“这一轮加载错文件”的错误。manifest 的价值主要就在这里，而不是把程序做得更高级。

所以我会把下一阶段明确切成两个独立 milestone。

第一步就是 Codex 说的阶段 A：只解决“一套 calibration 能否跨 session 复用”。这里方案基本合理。我只建议把 quick check 的目标再收紧一点。

quick check 不应该重新判断“这个人的 C-shape 做得标准不标准”，更不能重新估计动作幅度。它应该回答的是：

> 设备/手的位置有没有发生足以使原 calibration 失效的变化？

因此如果 session 本来从 Open 状态开始，第一版 quick check 可以主要检查：

> wrist 是否接近原 neutral；
> pinch/open distance 是否接近原 Open reference；
> 信号是否稳定。

如果你担心 C-shape reference 也漂，可以额外提示参与者短暂做一次 C-shape，再做一个 consistency check。但这仍然是“校验旧 calibration”，不是重新标定。

我甚至建议 calibration 文件不要失败后直接覆盖同一个文件，而是有版本：

```text
P001_exp2_cal_v01.json
P001_exp2_cal_v02.json
```

然后每个 session 的 summary 写：

```text
calibration_id: P001_exp2_cal_v01
```

如果 session 4 quick check失败，重新标定成 v02，那么以后你能非常明确地知道：

> session 1–3用了v01；
> session 4–6用了v02。

这比覆盖文件安全很多。

阶段 A 的验收标准就很简单：

> 第一次完整标定并保存；
> 第二次启动成功加载；
> quick check通过后不重新标定；
> 人为明显改变手/设备位置后quick check能失败；
> 失败后明确要求重新标定；
> 所有session知道自己用了哪个 calibration ID。

做到这里，A就结束。不要顺手加 manifest。

然后我认为可以马上做阶段 B。

### Manifest 我认为正式实验前应该有

而且不需要做成复杂平台。你贴出来的第一版范围已经够了。

它真正应该承担的是四件事：

> **谁在做；做什么；按什么顺序做；最后到底做了什么。**

因此我会让 manifest 成为整个 participant-level run 的“实验处方”。

例如：

```yaml
participant_id: P001
run_id: P001_exp2_001
run_seed: null

calibration:
  path: calibrations/P001_exp2_cal_v01.json
  reuse: true
  quick_check: true

sessions:
  - session_label: matrix_single_01
    order: 1
    task_type: single
    feedback_type: matrix_only
    config: only-matrix.yaml
    haptic_plan_config: haptic-plan-only-matrix-1.yaml

  - session_label: matrix_dual_01
    order: 2
    task_type: dual
    feedback_type: matrix_only
    config: only-matrix.yaml
    haptic_plan_config: haptic-plan-only-matrix-1.yaml
```

然后 runner 就只负责严格按照这张表执行。

我特别赞成第一版**不要自动决定实验顺序**。因为你真正的 counterbalancing 是实验设计问题，不应该偷偷藏在运行程序里。

以后你可以提前生成：

> P001的顺序
> P002的顺序
> P003的顺序

manifest 只负责忠实执行，并检查有没有明显错误。

这样方法学上也特别清楚：

> counterbalancing schedule 是预先生成的；
> manifest记录participant实际被分配到的schedule；
> runner执行该schedule。

### 但 manifest 应该比 Codex 当前方案多做几项很便宜的检查

不是复杂功能，而是防止低级实验错误。

例如启动前直接检查：

```text
participant_id 非空
session_label 唯一
task_type ∈ {single, dual}
feedback_type ∈ {combined, motor_only, matrix_only}
config 文件存在
plan 文件存在
plan_id 与声明一致
同一计划是否意外重复
calibration 文件状态
```

还应该检查一个非常实用的东西：

> `feedback_type` 和 config 是否明显矛盾。

比如 manifest写：

```text
feedback_type: matrix_only
```

但加载的是：

```text
only-motor.yaml
```

程序应该在实验开始前直接拒绝运行。

这种检查很值钱，因为正式实验最怕的不是模型少一个功能，而是30分钟以后发现选错条件。

### Seed 也很适合放到 manifest 层管理

你们前面已经把 session-level seed想清楚了。到了 manifest 后可以形成一个更清楚的层级：

```text
run_seed
   ↓
session_01_seed
   ├── haptic_seed
   └── nback_seed

session_02_seed
   ├── haptic_seed
   └── nback_seed
...
```

如果：

```yaml
run_seed: null
```

第一次运行自动生成并写进 `run_summary.json`。

如果以后要复现：

```yaml
run_seed: 381274
```

就能重新生成相同的伪随机计划。

这里同样不意味着manifest“自动设计实验”，它只是管理已经确定好的随机性。

### `run_summary.json` 很有必要

它不应该只是列output folder。我建议至少保存：

```text
participant_id
run_id
run_seed

calibration_id
calibration history

planned_session_count
completed_session_count

每个 session:
    session_label
    order
    task_type
    feedback_type
    plan_id
    config
    session_seed
    haptic_seed
    nback_seed
    output_path
    status
```

这样以后分析整个participant时，你不再需要：

> 打开18个文件夹，猜哪个是哪个。

你直接从 run root 开始分析。

---

所以我对“有没有必要做后面那部分”的回答是：

**有，而且我建议正式实验前一定做。**

不是因为manifest是高级功能，而是因为随着实验条件增多，人工管理本身已经开始成为数据质量风险。

但边界要守住。

这一版 manifest **不要做**：

> 自动Latin square；
> 自动决定哪个participant属于哪种counterbalance；
> 自动选择plan；
> 动态根据表现调整session顺序；
> 自动修改calibration；
> 自动删除失败session；
> 复杂GUI。

它只是一个：

> **配置校验器 + 顺序执行器 + 元数据记录器。**

这样工作量不会失控。

因此我会把接下来的开发顺序定成：

**Milestone A：Calibration reuse**

完成 `calibration save/load → quick check → 按需重新标定 → calibration version记录`。

通过测试以后立即进入：

**Milestone B：Participant run manifest**

完成 `participant/session定义 → config/plan校验 → 顺序运行 → seed管理 → run_summary`。

然后停止。

至于“让分析脚本直接吃run root”，可以算一个很小的 B2。因为一旦 manifest 有了，批量分析：

```bash
analyze_cue_response_metrics.py outputs/P001_exp2_001
```

自然就很值得做。

我的判断是，这两阶段甚至不需要隔一轮完整pilot。**A是B的底层能力，所以先单独实现和测试A；一旦A稳定，就可以马上实现B，然后拿下一位pilot第一次完整测试整个 participant-level workflow。**

这样下一位参与者测试的不只是某个detector，而是你正式实验真正会使用的工作流：

> 输入 participant ID
> → 完整标定一次
> → manifest按预定顺序跑session
> → session间quick check
> → 自动保存所有condition/plan/seed/calibration信息
> → 最后一个run root直接进入分析。

这个时候，程序才真正接近“可以放心采正式数据”的状态。
