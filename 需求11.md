当前 disabled 双任务测试已通过：
- calibration_passed=true
- total_haptic_events=5
- haptic_trial_count=1
- end_reason=haptic_release
- last_haptic_event_name=release
- haptic_events 时序已展开，不再同一 tick 发出
- warnings/errors 为空

现在进入真实 TCP 通讯测试阶段。不要改实验逻辑。

请确认 SimpleHapticSender 的真实 TCP 模式严格使用 vendor_exp2_abc 中的 vibration_tcp_worker、haptic_tcp_worker、vibration_haptic_protocol、matrix_haptic_protocol。
先实现/检查 enabled=true 时真实发送，required=false 时 warning 不中断，required=true 时失败中断。

测试顺序：
1. vibration_enabled=true, matrix_enabled=false
2. vibration_enabled=false, matrix_enabled=true
3. vibration_enabled=true, matrix_enabled=true

每个 haptic event 命令行打印 event name、modality、payload/channel、tcp status。
每个 haptic event 继续写入 haptic_events.csv 的 tcp_enabled、tcp_queued、tcp_success、send_status。
不要修改 exp2。
不要修改 manus_vive_com。
不要修改 MANUS TCP 8888 接口。